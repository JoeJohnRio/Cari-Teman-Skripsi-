<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/navy_blue_mid</item>
    </style>

    <style name="AppTheme.RoundedCornerMaterialButton" parent="Widget.AppCompat.Button"></style>

    <style name="ExposedDropdownMenu" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <!-- color of box ONLY, not dropdown -->
        <item name="boxBackgroundColor">@color/grey_low</item>
        <!-- supposed to set the background color of the dropdown menu - does nothing -->
        <item name="android:popupBackground">@color/grey_low</item>
        <!--
         Works partially, only the item background color is set
         leaving the actual background around the top and bottom showing
        -->
        <!-- <item name="android:background">@color/dialog_bg_color</item>-->
        <!-- does nothing -->
        <!-- <item name="android:colorBackground">@color/dialog_bg_color</item>-->
        <item name="boxStrokeColor">@color/orange_high</item>
        <item name="android:textColorHint">@color/orange_high</item>
        <item name="hintTextColor">@color/orange_high</item>
        <item name="android:textStyle">bold</item>
        <item name="errorEnabled">true</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="ExposedDropdownMenu.DropdownItem">
        <!-- sets the background of addFragmentWithBackStack single item, but background of dropdown still shows -->
        <!-- <item name="android:background">@color/dialog_bg_color</item>-->
        <item name="android:textColor">@color/grey_low</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="ratingBarCustom" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingbarstar</item>
        <item name="android:minHeight">22dip</item>
        <item name="android:maxHeight">22dip</item>
    </style>

    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/popup_background</item>
    </style>
</resources>
